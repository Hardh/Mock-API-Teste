{
  "name": "mock-api-teste",
  "version": "1.0.0",
  "description": "Mock de API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch server.json -p 4001 -d 1"
  },
  "author": "Alisson Dias",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2"
  }
}
